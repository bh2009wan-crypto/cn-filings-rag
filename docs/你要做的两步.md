# 你要做的两步（各约 15 分钟 + 40 分钟）

> A（财报问答知识库）已经全部做完并推上 GitHub。B 只剩这两件**必须你来**的事——不是脚本能代劳的。

---

## 第一步：Colab 上跑训练（约 40 分钟，其中大半是等它自己跑）

1. 打开本机文件 `~/work/homework3/notebooks/maotai_dongmi_lora.ipynb`，
   在 <https://colab.research.google.com/> 里 **文件 → 上传笔记本** 传上去。
   （或者：Colab 里 文件 → 打开笔记本 → 上传）

2. **上传 5 个数据文件**（Colab 左侧「📁 文件」图标 → 上传按钮）：
   | 本机路径 | 说明 |
   |---|---|
   | `~/work/homework3/data/sft/git/train.jsonl` | 训练集 262 条 |
   | `~/work/homework3/data/sft/git/test.jsonl` | 测试集 50 条 |
   | `~/work/homework3/data/sft/git/train_para.jsonl` | 训练集换问法 257 条 |
   | `~/work/homework3/data/sft/git/test_para.jsonl` | 测试集换问法 46 条 |
   | `~/work/homework3/data/sft/rag_context.json` | 格5（RAG 对照）的检索上下文 96 条 |

   > 仓库是私有的，raw 链接拉不到，所以必须手动上传这几个文件（第 3 格会检查缺哪些）。

3. **换成 GPU**：菜单 `代码执行程序 → 更改运行时类型 → 硬件加速器选 T4 GPU → 保存`。

4. **全部运行**：菜单 `代码执行程序 → 全部运行`（或 Ctrl+F9）。

5. 中途会看到：
   - 第 3 格：打印 train/test 的条数（应为 262 / 50 / 46 / 257）
   - 第 4 格：加载模型（第一次要下 4GB 左右，几分钟）
   - 第 5 格：打印 `将用于 LoRA 的 target_modules`（**不是空的**才能继续）
   - 第 7 格：训练前四格（约 12 分钟）
   - 第 8 格：训练（约 15 分钟，会滚动 loss）
   - 第 9 格：训练后四格（约 12 分钟）
   - 第 10 格：打印四格对照表，并**自动下载 3 个文件**

6. **跑挂了的应急**：
   | 现象 | 处理 |
   |---|---|
   | 第 7/8 格报模型相关错误（Qwen3.5-2B 是视觉+语言模型，LoRA 兼容有风险） | 把第 4 格 `PRIMARY` 改成 `"Qwen/Qwen3-1.7B"`，再全部运行 |
   | 报 CUDA out of memory | 第 8 格 `per_device_train_batch_size` 改 1、`gradient_accumulation_steps` 改 16 |
   | 跑了很久没动静 / 断线 | 从头全部运行即可；长跑前可以把 `/content/dongmi_lora` 挂到 Google Drive |
   | 第 3 格说缺文件 | 回第 2 步重新上传，注意文件名要一模一样 |

7. **回传**：把下载到本机的 `results_b.json` 放进 `~/work/homework3/outputs/eval/`，
   然后告诉我（或自己跑）：
   ```bash
   cd ~/work/homework3 && /Users/lionel/anaconda3/envs/crawler/bin/python scripts/22_run_eval_b.py
   ```
   它会生成 `outputs/B_对照表.md`，我再把 `docs/结论B_一页.md` 第 4 节填上。

---

## 第二步：人工抽检 40 条（约 15 分钟）

课件明写「**人工抽检，删掉编造的**」——这一步是交付物，脚本不能替你签字。

1. 打开 `~/work/homework3/data/sft/review_40.csv`（用 Excel 或 Numbers 都行）。
2. 看这四列就够：`问题` → `答案` → `依据原文` → `块原文(供核对)`（最后几列已填好，**不用翻 PDF**）。
3. **只填三列**：`判定(留/删)`、`编造类型`（编数字 / 编事实 / 口径错 / 答非所问）、`备注`。
4. 判定标准（唯一标准）：**答案里任何一个数字、任何一个事实，只要不能在那条「依据原文」或所在块原文里找到，就算编造，删。**
5. **优先看这 14 条**（依据不是逐字摘录的，最可能出问题）：序号
   `33, 6, 2, 23, 4, 8, 28, 17, 24, 38, 5, 26, 29, 19`
   （其中 `6 / 19 / 24 / 28 / 38` 是会计政策类问题——最容易把准则原文引申成自己的话）
   详细清单和每条的"为什么被标出来"见 `docs/抽检记录.md` 第二节。
6. 判完把统计填进 `docs/抽检记录.md` 第三节（抽检条数 / 判留 / 判删 / **编造率**）+ 第四节写 3 个典型编造案例。
7. **如果删了条目**：告诉我，我会重跑 `13_split.py` + `14_make_paraphrase.py`（否则训练/测试划分与数据就不一致了），你再重新上传数据跑 Colab。

---

## 做完这两步之后

`docs/结论B_一页.md` 的第 4 节填上四格数字，作业 3 就齐了。到时候一起再推一次 GitHub。
