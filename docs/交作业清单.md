# 交作业清单（全部工作已完成，这里是交付与可选加分项）

> 原来的 Colab 步骤已经不需要了（网络卡死，改本机 M2 跑完，见 `docs/结论B_一页.md` 第七节）。
> 人工抽检也已完成，结果在 `docs/抽检记录.md`。
>
> **方向 A 交平台的提交包**：跑 `python scripts/24_make_submission_A.py` → 生成 `提交_A/`
> （可直接粘贴的 `作业3_A_提交材料.md` + 两张截图）。方向 B 若要交，按下面第一张表找文件。

## 一、交付物 ↔ 文件（交作业时按这张表找）

| 课件要求的交付物 | 文件 | 状态 |
|---|---|---|
| **A · 代码仓库** | 本仓库（**已公开**）：`github.com/bh2009wan-crypto/cn-filings-rag` | ✅ |
| **A · 页面截图** | `outputs/A_页面截图.png`（首页）、`outputs/A_页面截图_回答.png`（回答 + 证据卡片） | ✅ |
| **A · 一页结论** | `docs/结论A_一页.md` | ✅ |
| **A · 10 题逐题记录** | `outputs/eval/results_a.csv`（含召回块、角标、数字核对、人工判分 verdict/error_type） | ✅ |
| **A · 冻结的题与参考要点** | `eval/questions_a.json`、`eval/questions_a.sha256`（冻题哈希）、`eval/key_facts.json` | ✅ |
| **B · 造数据脚本** | `scripts/10_gen_qa.py`（生成）、`scripts/11_qc_qa.py`（质检）、`scripts/12c_add_citation.py`（补依据） | ✅ |
| **B · 人工抽检记录** | `docs/抽检记录.md` + `data/sft/review_40.csv`（40 条逐条判定） | ✅ |
| **B · 训练笔记本** | `notebooks/maotai_dongmi_lora.ipynb`（Colab 版，可直接跑）<br>＋ `scripts/23_local_sft_mps.py`（本机 M2 版，实际用的是这个） | ✅ |
| **B · 四格对照表** | `outputs/B_对照表.md` | ✅ |
| **B · 一页结论** | `docs/结论B_一页.md` | ✅ |
| 附 · 踩坑复盘（不在要求里） | `docs/踩坑复盘.md`（14 条，都带实测数字） | ✅ |

## 二、仓库已公开

`https://github.com/bh2009wan-crypto/cn-filings-rag`（2026-09-24 由 homework3 改名并公开，
旧链接会自动重定向）。公开前已核查：`.env` 从未入库、无密钥、无 PDF/向量/适配器。
Topics 已加：llm / rag / bm25 / lora / sft / financial-reports / chinese-nlp / apple-silicon。

## 三、可选加分项（不做也完整）

1. **Colab 上再跑一版 2B**：网络稳定时，把 `notebooks/maotai_dongmi_lora.ipynb` 传上 Colab，
   数据文件在 `data/sft/git/`（4 个 jsonl）+ `data/sft/rag_context.json`，Run all 约 40 分钟。
   基座换成课件同款 Qwen3.5-2B，和我们的 0.6B 版对着看——**四格口径完全一致，可以直接比**。
   预期：口吻类指标还会涨，但"格式易学、事实难学"的结构不会变（这正是要展示的结论）。
2. **把 `docs/踩坑复盘.md` 的 14 条**发给同学——那里面的坑（巨潮 orgId、表格文字重复、页码偏移、
   hf-mirror 只有 7.5KB/s、Colab 连不上的判据脚本）能省掉别人一整天。
3. **问答页面留着当 demo**：`python scripts/08_app.py --port 7860` → 打开 `http://127.0.0.1:7860`，
   10 道题按钮点一下就问，回答带角标与证据卡片。
